/**
 * Layer 2 — Roto-PGN parse/serialize.
 *
 * Canonical long form: piece letter + from + (- | x) + to, with suffixes
 * (* halo, † evaporation, ^ avenger, =Q/R/B/N, e.p., O-O) and "&"-joined
 * opening turns. Interchange/export format only — never the storage format.
 *
 * Filled in at milestone M3.
 */

export {};
