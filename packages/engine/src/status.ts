/**
 * Layer 3 — Status: check, checkmate, stalemate, draw detection.
 *
 * §7.3 timing rule: it is not checkmate until the threatened player is to
 * move and has no legal turn. Terminal evaluation is a function of
 * (state, player-to-move), run for the NEW active player after every turn.
 *
 * Filled in at milestone M2.
 */

export {};
