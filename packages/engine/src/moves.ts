/**
 * Layer 2 — Move & Turn objects.
 *
 * The TURN is the atomic unit of submission, validation, storage, and
 * notation. During the opening (plies 1–20) a turn is two submoves, one on
 * each side of the mover's own Meridian; afterwards it is one.
 *
 * Filled in at milestone M2.
 */

export {};
