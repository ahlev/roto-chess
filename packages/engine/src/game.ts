/**
 * Layer 4 — Game: metadata, playGame fold, replay.
 *
 * A game is initialState + an ordered list of turns; every derived view
 * (current position, scrubbing, catch-up replay, export) is the same fold.
 *
 * Filled in at milestone M3.
 */

export {};
