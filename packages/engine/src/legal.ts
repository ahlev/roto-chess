/**
 * Layer 3 — Legality: per-piece move generators, legalTurns, applyTurn.
 *
 * The engine's public API is legalTurns(state) / applyTurn(state, turn);
 * single-submove application is internal. Halo, evaporation, Avenger, en
 * passant, both castling forms, and the self-check filter live here.
 *
 * Filled in at milestone M2.
 */

export {};
